<% layout('boilerplate/boilerplate') %>
<div class="conatiner-fluid ">
  <div class="row m-2">
    <div id="map" style='width: 100%; height: 500px;'></div>
    <h1 class=" text-center fw-bold">Our Services</h1>
    <% for ( service of services) { %>

    <div class="col-12 mt-2 services">
      <a href="/services<%= service.link %>">
        <div class="card mb-3" >
          <div class="row g-0">
            <div class="col-md-4">
              <img src="<%= service.image %>" class="img-fluid rounded-start servicesImg" alt="..." />
            </div>
            <div class="col-md-8">
              <div class="card-body">
                <h5 class="card-title text-center"><%= service.title %></h5>
                <p class="card-text ">
                  <%= service.text %>
                </p>
              </div>
            </div>
          </div>
        </div>
      </a>
    </div>
    <% } %>
  </div>
</div>

<script>
    const mapToken ="<%- process.env.MAP_BOX_TOKEN %>";
    const user = { features : <%- JSON.stringify(indServices)%>} ;
</script>

<script src="/javascript/clusterMap.js"></script>